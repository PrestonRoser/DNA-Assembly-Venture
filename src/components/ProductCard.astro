---
const etsy = "https://www.etsy.com/listing/4368469267/3d-dnarna-assembly-kit-stem-learning-kit?etsrc=sdt";
---
<section class="section" style="padding-top:0">
  <div style="display:flex;justify-content:center">
    <div style="display:grid;grid-template-columns:repeat(2,minmax(260px, 420px));gap:1.25rem">

      <!-- Card 1: Current product (entire card clickable) -->
      <div class="card card-hover" style="position:relative;text-align:center;padding:1.25rem 1.25rem 1.5rem">
        <!-- Stretched link overlay -->
        <a href="/product" aria-label="View DNA Assembly Kit" style="position:absolute;inset:0;border-radius:inherit;z-index:1"></a>

        <div style="width:100%;aspect-ratio:1/1;border:1px solid var(--border);border-radius:16px;overflow:hidden;display:flex;align-items:center;justify-content:center;background:#fafbff">
          <img src="/images/assembled-student-kit.png" alt="Assembled student DNA and RNA kit" style="max-width:84%;height:auto;object-fit:contain;transition:filter .2s ease, transform .2s ease;" />
        </div>

        <h2 class="h2" style="margin:1rem 0 .25rem">Featured product: DNA Assembly Kit</h2>
        <p class="muted" style="margin:.25rem 0 0">Color-coded, reusable parts to model base pairing, antiparallel strands, and transcription.</p>
        <ul class="muted" style="margin:.35rem auto 0; text-align:left; max-width:440px">
          <li>• Teacher ready activities, 15-45 minutes</li>
          <li>• NGSS aligned concepts</li>
          <li>• Classroom safe and durable</li>
        </ul>

        <!-- Buttons sit above the overlay so they are still clickable as buttons -->
        <div style="position:relative;z-index:2;display:flex;gap:.6rem;flex-wrap:wrap;justify-content:center;margin-top:.9rem">
          <a class="btn-primary" href="/product">View product</a>
          <a class="btn-link" href="/contact">Request a quote</a>
        </div>
      </div>

      <!-- Card 2: Coming soon -->
      <div class="card card-hover" style="position:relative;text-align:center;padding:1.25rem 1.25rem 1.5rem">
        <a href="/venture" aria-label="See upcoming kits on Venture page" style="position:absolute;inset:0;border-radius:inherit;z-index:1"></a>

        <div style="width:100%;aspect-ratio:1/1;border:1px dashed var(--border);border-radius:16px;display:flex;align-items:center;justify-content:center;background:#f6f7fb">
          <div class="muted" style="font-weight:600">New kits in development</div>
        </div>

        <h2 class="h2" style="margin:1rem 0 .25rem">More Hands kits coming</h2>
        <p class="muted" style="margin:.25rem 0 0">We are prototyping additional tactile STEM kits. Follow along and join our pilots.</p>

        <div style="position:relative;z-index:2;display:flex;gap:.6rem;flex-wrap:wrap;justify-content:center;margin-top:.9rem">
          <a class="btn-primary" href="/venture">View roadmap</a>
          <a class="btn-link" href="/contact">Partner with us</a>
        </div>
      </div>

    </div>
  </div>
</section>

<style>
  /* Scoped hover/tint effect for both cards */
  .card-hover { position: relative; overflow: hidden; }
  .card-hover::after {
    content: "";
    position: absolute; inset: 0;
    background: linear-gradient(180deg, rgba(38,103,255,0.08), rgba(172,234,211,0.10));
    opacity: 0;
    transition: opacity .2s ease;
    border-radius: inherit;
  }
  .card-hover:hover::after { opacity: 1; }
  .card-hover:hover img { filter: saturate(1.05) hue-rotate(6deg) contrast(1.02); transform: translateY(-1px); }
</style>

